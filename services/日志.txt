

---------------------------------------------------------------------------------------
version 2025/03/19 by wjy  
---------------------------------------------------------------------------------------
以下是重新排版后更清晰、简洁的更新内容：  

---

### **更新内容**  

1. **组件优化**  
   - 改进了组件以更好地显示词书信息。  
   - 添加了默认图标，改进了布局和样式。

---

### **功能更新**  

#### **2. 文件：`book_select/page.tsx`**  
   - **新增功能**：
     - 从后端获取词书列表。  
     - 实现词书搜索功能。  
     - 添加加载状态和错误处理。  
     - 显示每本词书的单词数量。  

#### **3. 文件：`app/book/[id]/page.tsx`**  
   - **新增词书详情页面**：
     - 显示词书信息和单词列表。  
     - 添加学习和复习按钮。  
     - 实现加载状态和错误处理。  

---

### **服务层修改：`vocabulary-service`**  

#### **4. 基础功能修改**  
   - **更新 `checkVocabularyTables` 函数**：  
     - 现在检查三个表：`word_list`、`book_list`、`Book_word_mapping`。  
   - **修改 `initializeDatabase` 函数**：  
     - 确保所有必要表都存在。  

#### **5. 单词相关功能修改**  
   - **更新 `getVocabularyWords` 函数**：  
     - 支持按词书 ID 过滤单词。  
   - **修改 `addVocabularyWord` 函数**：  
     - 支持同时将单词添加到特定词书。  
   - **更新 `deleteVocabularyWord` 函数**：  
     - 删除单词时同时删除相关映射关系。  

#### **6. 新增词书相关功能**  
   - 添加 `getAllBooks` 函数：获取所有词书。  
   - 添加 `getBookById` 函数：获取特定词书的详细信息。  
   - 添加 `getBookWordCount` 函数：获取特定词书中的单词数量。  
   - 添加 `addWordToBook` 和 `removeWordFromBook` 函数：管理词书中的单词。  
   - 添加 `createBook` 和 `deleteBook` 函数：创建和删除词书。  

---

### **类型定义更新**  

#### **7. 文件：`types/vocabulary.ts`**  
   - 添加新类型定义：`VocabularyBook` 和 `BookWordMapping`。  
   - 更新 `VocabularyWord` 接口，添加可选字段：`pronunciation`。  


   ------------------------------------------------------------------------------------
---------------------------------------------------------------------------------------
version 2025/04/2 by zyl  
---------------------------------------------------------------------------------------
用户注册服务函数添加相同邮箱注册时，返回邮箱已经存在的错误信息。 

Integration_modules\user-page-button.tsx：
主要改进：
添加了密码长度验证（至少6个字符）
优化了邮箱重复注册的错误处理：
显示更友好的错误标题和描述
自动切换到登录选项卡
提示用户直接登录或使用其他邮箱
注册成功后清空表单数据
根据不同的错误类型显示不同的提示信息
现在当用户尝试使用已注册的邮箱时：
会看到明确的错误提示："邮箱已存在"
会收到建议："该邮箱已被注册，请直接登录或使用其他邮箱"